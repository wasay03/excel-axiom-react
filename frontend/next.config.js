/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options go here.
  // For a basic setup, it can be empty.
  // reactStrictMode: true, // This is a common default option
};

module.exports = nextConfig;